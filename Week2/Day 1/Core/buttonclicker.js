function disappear() {
   var button = document.getElementsByClassName("def")[0];
button.remove();
}

function logout() {
    var button = document.getElementsByClassName("btn")[0];
    button.innerHTML = "logout";
    
}